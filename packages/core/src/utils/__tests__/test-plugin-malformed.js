module.expss Test {
  /** The name of the plugin */
  name = 'foo';

  /** The options of the plugin */
  config: {};

  /** Initialize the plugin with it's options */
  constructor(config: {}) {
    this.config = config;
  }
};
